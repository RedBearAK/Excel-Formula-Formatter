# Core terminal UI
rich>=13.0

# Full-screen Textual editor (default UI); [syntax] pulls tree-sitter
# grammars for JavaScript-mode highlighting
textual[syntax]>=1.0

# Linux clipboard (native packages, not pip):
#   Wayland: wl-clipboard
#   X11:     xclip or xsel
